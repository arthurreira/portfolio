---
title: "AF Next.js Component Architecture"
description: "A practice project exploring atomic design principles in Next.js, using pnpm workspaces and TypeScript to build a scalable, well-structured component library."
createdAt: "2026-08-12"
featured: false
role: "solo"
highlight: "Monorepo component library — pnpm workspaces, atomic design hierarchy, strict TypeScript props"
techStack:
  - "Next.js"
  - "TypeScript"
  - "pnpm workspaces"
  - "Tailwind CSS"
githubRepo: "https://github.com/arthurreira/af-nextjs-component-architecture-practice"
url: "https://github.com/arthurreira/af-nextjs-component-architecture-practice"
status: "done"
---

## What I built

A monorepo component library built to practice the atomic design methodology from first principles. Two workspaces: a `packages/ui` shared library where every component lives, and an `apps/playground` Next.js app used to render and test them. The hierarchy runs atoms → molecules → organisms, each layer only composing from the layer below. All component props are strictly typed with TypeScript interfaces — no implicit `any`.

## What I learned

The main insight was that atomic design is a communication tool as much as a technical pattern. The discipline of asking "is this an atom or a molecule?" forced clearer thinking about component responsibility. The pnpm workspace setup also taught me how shared packages are resolved differently from npm packages — and why monorepos require explicit dependency declarations between workspaces rather than relying on hoisting.

## Structure

```
packages/
  ui/           # Shared component library (atoms, molecules, organisms)
apps/
  playground/   # Next.js app for showcasing and testing components
```

## Key Concepts Practiced

- **Atomic design** — components grouped by complexity and responsibility
- **pnpm workspaces** — monorepo setup with shared packages
- **TypeScript** — strict prop typing and reusable interface definitions
- **Tailwind CSS** — utility classes applied at the component level
