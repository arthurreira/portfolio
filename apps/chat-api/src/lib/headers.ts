/**
 * Set when the answer came from the fallback model, so the UI can say so
 * rather than passing off a visibly weaker answer as the real thing.
 */
export const DEGRADED_HEADER = "x-chat-degraded"
